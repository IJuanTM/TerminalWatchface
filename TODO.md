TODO:

- Add different date display options (Mon, 1 Jan; ymd; dmy; mdy)
- Add different battery display options (percentage; days; percentage + days)
- Add more combined weather options
- Manual layer swiching? Toggle auto switch on/off.
  - Maybe even a screen system with each their own layer settings so a user can switch between time, date, steps, calories, heart rate, battery and time, date, temperature, weather condition, precipitation, battery. For example.
  - And then each screen can also still have their own layers per row. 
  - 3 screens, 3 layers per row, max 7 rows. Time and date are static on first screen, other screens can be fully customized.
  - Swithing should be done by double tapping or swiping left to right/right to left goes from screen 1 -> 2 -> 3 -> 1 or 3 -> 2 -> 1 -> 3
- Check why weather condition is [?] on actual watch outside of simulator