[ ] Make gradient not a type, but a color option, add different color options (green, yellow/orange, red (for HR, Stress for example) (blue, green, yellow, orange, red, magenta (for weather forecast for example)) etc. Also reversed options should be available, for example body battery, where low should be red and high values should be green.
[x] Fix graphs not shown when not enough data? When option is graph, it should always render, even with no data to show yet (maybe even a centered text in tiny size, light gray, saying "no data to show" or something
[ ] Fix holes in line, gradient line graph rendering
[x] When user has chosen Graph + Value and also configured a secondary graph in the same graph, it should show both current values, maybe in small font size above one another.
[ ] Steps graph? Not based on any time or historic data, but based on 0 -> goal e.g. 10000 and then have a bar fill from left to right to show progress. This could be a new graph type that can be used for all types of "goal" related things. Just like with other graphs, color should be able to be set, and if it should just be the graph or if the current value should also be shown to the right.
[ ] Show mean/max values in graph when available, for example with HR. Maybe a dashed line in the background or something.
[ ] Moment screens? For example in the morning it could show a special customised screen with things like sleepscore, todays forecast, next calendar event. And then the user can "dismiss" that screen by long pressing on the watchface, or it should dissappear after a set period after showing (for example show at 7:00 and for 15 minutes, two different settings). Maybe also an "after workout screen" could be possible, I don't know if that is detectable. And maybe an "after sleep screen" so when the user woke up.
[x] Fix body battery rounding, too many decimals.
